/*
for (let index = 0; index < colors.length; index += 1) {
  let selectedColor = colors[index];
  const colors = document.createElement('div');
  colors.innerText = selectedColor;
  colorPalletElement.appendChild(colors);
}
*/
