/*window.onload = function () {
  function createBoard (rows, colunms) {
    const myBoard = document.getElementById('pixel-board');

    for (let index = 0; index < rows; index += 1) {
      let row = document.createElement('div');

      for (let indexCol = 0; index < colunms; indexCol += 1) {
      let myColunms = document.createElement('div');
      myColunms.classList.add('pix')
      rows.appendChild(mycolunms);
      }
      myBoard.appendChild(row);
    }
  }
  console.log(createBoard(5, 5));
}
*/